var express = require('express');
var path = require('path');
var favicon = require('serve-favicon');
var logger = require('morgan');
var cookieParser = require('cookie-parser');
var bodyParser = require('body-parser');
var stylus = require('stylus');
var fs = require('fs');

var mongoose = require('mongoose');


var schemas = require('./comic');
var index = require('./routes/index');
var users = require('./routes/users');
var comic = require('./routes/comics');
var view_comics = require('./routes/view_comics');
var chapter_selection = require('./routes/chapter_selection');

var series = schemas.series;
var chapters = schemas.chapter;
var pages = schemas.page;

var app = express();

// view engine setup

mongoose.connect('mongodb://localhost/comics');


app.set('views', path.join(__dirname, 'views'));
app.set('view engine', 'jade');

// uncomment after placing your favicon in /public
//app.use(favicon(path.join(__dirname, 'public', 'favicon.ico')));
app.use(logger('dev'));
app.use(bodyParser.json());
app.use(bodyParser.urlencoded({ extended: false }));
app.use(cookieParser());
app.use(stylus.middleware(path.join(__dirname, 'public')));
app.use(express.static(path.join(__dirname, 'public')));

app.use('/', index);
app.use('/users', users);
app.use('/comic', comic);
app.use('/comic_:id', chapter_selection);
app.use('/comic_:id/chapter_:id', view_comics);


// catch 404 and forward to error handler
app.use(function(req, res, next) {
  var err = new Error('Not Found');
  err.status = 404;
  next(err);
});

// error handler
app.use(function(err, req, res, next) {
  // set locals, only providing error in development
  res.locals.message = err.message;
  res.locals.error = req.app.get('env') === 'development' ? err : {};

  // render the error page
  res.status(err.status || 500);
  res.render('error');
});



module.exports = app;
